/////////////////////////////////////////////////////////////////////////////
// PROJECT:		HACCDll
// FILE:		$Workfile: Cioport.cpp $
// ARCHIVE:		$Archive: /Project/Units/Tasha(Developer)/HAccDll/Cioport.cpp $
// CHECKIN:		$Date: 5.01.04 9:52 $
// VERSION:		$Revision: 1 $
// LAST CHANGE:	$Modtime: 5.01.04 9:25 $
// BY AUTHOR:	$Author: Martin.lueck $
// $Nokeywords:$
//////////////////////////////////////////////////////////////////////

#include "stdafx.h"


#ifdef _DEBUG
#undef THIS_FILE
static char BASED_CODE THIS_FILE[] = __FILE__;
#endif

///////////////////////////////////////////////////////////////////////////////////////////////////////////
CIoPort::CIoPort(DWORD dwIOBase) : CIo(dwIOBase)
{
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////
CIoPort::~CIoPort()
{
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////
BYTE CIoPort::Input8(DWORD dwPort) const
{
	return HACCReadPort8(dwPort, FALSE);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////
WORD CIoPort::Input16(DWORD dwPort) const
{
	return HACCReadPort16(dwPort, FALSE);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////
DWORD CIoPort::Input32(DWORD dwPort) const
{
	return HACCReadPort32(dwPort, FALSE);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////
void CIoPort::InputNN(void* /*pDestBuffer*/, DWORD /*dwPort*/, DWORD /*dwLen*/) const
{
	TRACE("CIoPort::InputNN not implemented\n");
	//return HACCReadPortNN(dwPort, FALSE);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////
void CIoPort::Output8(DWORD dwPort, BYTE byData) const
{
	HACCWritePort8(dwPort, byData, FALSE);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////
void CIoPort::Output16(DWORD dwPort, WORD wData) const 
{
	HACCWritePort16(dwPort, wData, FALSE);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////
void CIoPort::Output32(DWORD dwPort, DWORD dwData) const 
{
	HACCWritePort32(dwPort, dwData, FALSE);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////
void CIoPort::OutputNN(void* /*pSrcBuffer*/, DWORD /*dwPort*/, DWORD /*dwLen*/) const
{
	TRACE("CIoPort::OutputNN not implemented\n");
	//return HACCReadPortNN(dwPort, FALSE);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////
void CIoPort::OutputLatch8(DWORD dwPort, BYTE byData) const
{
	HACCWritePort8(dwPort, byData, TRUE);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////
void CIoPort::OutputLatch16(DWORD dwPort, WORD wData) const
{
	HACCWritePort16(dwPort, wData, TRUE);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////
void CIoPort::OutputLatch32(DWORD dwPort, DWORD dwData) const
{
	HACCWritePort32(dwPort, dwData, TRUE);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////
BYTE CIoPort::InputLatch8(DWORD dwPort) const
{
	return HACCReadPort8(dwPort, TRUE);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////
WORD CIoPort::InputLatch16(DWORD dwPort) const
{
	return HACCReadPort16(dwPort, TRUE);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////
DWORD CIoPort::InputLatch32(DWORD dwPort) const
{
	return HACCReadPort32(dwPort, TRUE);
}
